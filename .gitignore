# stack principal: Go (ver docs/BETA0-PLAN.md) — mas scripts/ tem tooling Node auxiliar
.DS_Store
*.swp
.env
.env.local
*.db
*.sqlite

# binários de build do Go — `go build ./...` sem -o escreve na raiz
/appview
/cmd/*/*.exe

# tooling Node auxiliar (ex: scripts/dev-pds), não faz parte do app Go
node_modules/
