{
  "lexicon": 1,
  "id": "social.orbita.note",
  "defs": {
    "main": {
      "type": "record",
      "description": "A note about a work, optionally anchored to a specific season/episode.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["work", "text", "createdAt"],
        "properties": {
          "work": {
            "type": "ref",
            "ref": "social.orbita.shelf.item#work",
            "description": "Reuses shelf.item's work def instead of duplicating it — same {provider, id} shape."
          },
          "season": {
            "type": "integer",
            "minimum": 1,
            "description": "TV only. If set without episode, the note is about the season as a whole."
          },
          "episode": {
            "type": "integer",
            "minimum": 1,
            "description": "TV only. Requires season to also be set — enforced by the appview, not this schema."
          },
          "text": {
            "type": "string",
            "minLength": 1,
            "maxLength": 3000
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          },
          "reply": {
            "type": "ref",
            "ref": "#replyRef",
            "description": "Present when this note is a reply to another note. root and parent are the same record when replying directly to a root-level note; root stays the original thread's root when replying to a reply."
          }
        }
      }
    },
    "replyRef": {
      "type": "object",
      "required": ["root", "parent"],
      "properties": {
        "root": { "type": "ref", "ref": "com.atproto.repo.strongRef" },
        "parent": { "type": "ref", "ref": "com.atproto.repo.strongRef" }
      }
    }
  }
}
